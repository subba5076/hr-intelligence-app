# Not required for local dev (Vite proxies /api to the backend, see vite.config.ts).
# Set this only if the frontend needs to call a backend on a different host,
# e.g. when built and deployed separately from the API.
VITE_API_BASE_URL=

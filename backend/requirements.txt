# Versions below were verified to install and work together (imports +
# a full local smoke test of the API, DB, and RAG pipeline) as of this
# project's creation. If you bump versions later, rerun the backend tests.

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.1

# --- Settings / validation ---
pydantic==2.13.4
pydantic-settings==2.14.2

# --- Database (PostgreSQL) ---
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
# Note: no Alembic migrations yet -- app.db.database.init_db() creates
# tables directly for simplicity. For a real production system, replace
# that with Alembic-managed migrations (`pip install alembic`) so schema
# changes are versioned and reviewable.

# --- RAG pipeline ---
langchain==1.3.14
langchain-community==0.4.2       # FAISS + HuggingFaceEmbeddings wrappers
langchain-openai==1.4.1
langchain-text-splitters==1.1.2
faiss-cpu==1.15.0
sentence-transformers==3.3.1     # local, free embedding model (no API key needed)
tiktoken==0.13.0
pypdf==6.15.0                    # extracts text from uploaded PDF HR documents

# --- Utilities ---
python-dotenv==1.2.2
python-multipart==0.0.32         # file uploads (documents)
httpx==0.28.1

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0
